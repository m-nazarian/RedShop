# مرحله هجدهم: پاک‌سازی import در shop/services.py

در این مرحله import باقی‌مانده از اسکن مرحله شانزدهم دوباره با AST بررسی شد.

## نتیجه بررسی

- `Category` از `.models` import شده بود اما در `apps/shop/services.py` استفاده نمی‌شد.
- import اضافه حذف شد.

## نتیجه

پاک‌سازی به‌صورت محدود و قابل تست انجام شد تا رفتار runtime پروژه تغییر نکند.
